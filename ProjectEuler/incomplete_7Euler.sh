#!/bin/bash

# Author: J. J. Abraham
# Date: 16 June 2018 ( Youth Day (: )
# Description: Calculate what the 10 001st prime number.

#LIST_OF_PRIMES 

isPrime() {
	# Populate LIST_OF_PRIMES until last element is greater than parsed variable.
	# Check whether passed variable is in LIST_OF_PRIMES.
	# Keep LIST_OF_PRIMES as a persistant list.
}

count=6
i=13

while test $count != 10001
do
	
	((count ++))
done
